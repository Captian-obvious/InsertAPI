import base64,os,sys,rbxm,requests,robloxapi

def parseObject():
    if isinstance(object, rbxm.UnionOperation):
        parts = object.Parts
    ##endif
##end

def parseAsset(data):
    class data:
        isFinished = False
    ##end
    file = rbxm.load(data)
    for object in file.objects:
        parseObject(object,data)
    ##end
##end
